{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Fix broken gallery assets and publish Draft Version 6",
  "requirements": [
    {
      "id": "REQ-17",
      "summary": "Ensure the UI-referenced uploaded images exist under frontend/public/assets/generated so they are publicly served as static assets.",
      "acceptanceCriteria": [
        "`frontend/public/assets/generated/1770877868282~2-2.png` exists in the repository and is readable as a static asset.",
        "`frontend/public/assets/generated/1770877868282~2-3.png` exists in the repository and is readable as a static asset.",
        "Visiting `/assets/generated/1770877868282~2-2.png` and `/assets/generated/1770877868282~2-3.png` on the deployed site returns the image content (not a 404)."
      ],
      "file_operations": [
        {
          "path": "frontend/public/assets/generated/1770877868282~2-2.png",
          "operation": "create",
          "description": "If missing, copy/move the existing uploaded file from the repository root upload directory into this exact path so it is served as a public static asset."
        },
        {
          "path": "frontend/public/assets/generated/1770877868282~2-3.png",
          "operation": "create",
          "description": "If missing, copy/move the existing uploaded file from the repository root upload directory into this exact path so it is served as a public static asset."
        }
      ]
    },
    {
      "id": "REQ-18",
      "summary": "Update frontend image src references to point to valid public /assets/generated paths for the moved images and eliminate any remaining references to missing files.",
      "acceptanceCriteria": [
        "`frontend/src/components/CategoryGrid.tsx` image paths resolve to valid public URLs under `/assets/generated/` for Baby Booties and Hair Crochet Clips (and any other images referenced by the page).",
        "On the deployed site, the category grid shows actual images (no broken image icons) for Baby Booties and Hair Crochet Clips.",
        "There are no console errors in the browser related to failing to load images from `/assets/generated/` (404/ERR_NAME_NOT_RESOLVED)."
      ],
      "file_operations": [
        {
          "path": "frontend/src/components/CategoryGrid.tsx",
          "operation": "modify",
          "description": "Verify all category image src values reference existing static assets under `/assets/generated/` (including `1770877868282~2-2.png` and `1770877868282~2-3.png`) and update any incorrect/missing references; keep changes limited to image path fixes and do not modify immutable `frontend/src/components/ui/*`."
        },
        {
          "path": "frontend/src/App.tsx",
          "operation": "modify",
          "description": "Audit and correct any <img> or CSS backgroundImage references to ensure they use valid public paths under `/assets/generated/` and do not point to missing files; do not change non-image content beyond what is necessary."
        }
      ]
    },
    {
      "id": "REQ-19",
      "summary": "Bump draft marker to Version 6 after verifying asset placement and image src fixes.",
      "acceptanceCriteria": [
        "`frontend/public/draft-version.txt` contains `Draft Version 6`.",
        "Draft Version 6 is deployed and the live site renders the updated images correctly."
      ],
      "file_operations": [
        {
          "path": "frontend/public/draft-version.txt",
          "operation": "modify",
          "description": "Update the marker text from `Draft Version 5` to `Draft Version 6`."
        }
      ]
    }
  ]
}